import * as React from 'react';
import Layout from '../components/Layout/Layout';

const Contact = () => {
    return (
        <Layout>
            <section>
                <h1>Contact</h1>
            </section>
        </Layout>
    );
};

export default Contact;
