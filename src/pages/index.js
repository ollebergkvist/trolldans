import * as React from 'react';
import Layout from '../components/Layout/Layout';

// markup
const IndexPage = () => {
    return (
        <Layout>
            <section>
                <h1>Trolldans</h1>
            </section>
        </Layout>
    );
};

export default IndexPage;
